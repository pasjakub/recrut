CREATE DATABASE IF NOT EXISTS jakub;
USE jakub;
CREATE TABLE IF NOT EXISTS osoba (id INT AUTO_INCREMENT, imie VARCHAR(15), nazwisko VARCHAR(30), PRIMARY KEY (id));
CREATE TABLE IF NOT EXISTS dane (dane_id int auto_increment, plec VARCHAR(20), wiek int, primary key (dane_id), FOREIGN KEY (osoba_id) REFERENCES osoba(id));
